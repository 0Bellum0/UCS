package filme;

import java.util.Comparator;

public class FilmeComparator implements Comparator <Filme> {

	@Override
	   public int compare(Filme f1.getNome(), Filme f2) {
	       return f1.getNome().compareTo(f2.getNome());
	   }	
}